using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace BadgeLibrary
{
    public class BadgeRepository
    {
        Dictionary<int, Badge> _repo = new Dictionary<int, Badge>();
        //Dictionary<Badge.BadgeId, Badge> _repo = new Dictionary<Badge.BadgeId, Badge.DoorNames>();


        //Methods

        //Create/ AddNewBadge
        //Probably will use Add(TKey, TValue)-- not sure where badgeName falls into this //_repo.Add(badge.BadgeId, badge)

        //Read / GetAllBadges
        //

    }
}